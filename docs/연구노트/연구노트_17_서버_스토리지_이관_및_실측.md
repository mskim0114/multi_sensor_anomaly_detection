# 연구노트 #17: 서버 스토리지 이관 및 서버 실측 (2026-09-17 ~ 18)

- **연구 질문 및 hypothesis ID:** 해당 없음 (인프라 작업). gate 로는 G0(서버 현황) · G1-S 선행조건 · B-1/B-3 해소.
- **시작 전 알고 있던 사실:** 09-17 preflight(`~/review_runs/20260917_monitoring/server_preflight.json`)에서
  서버 루트 디스크 97 % 사용, nvidia-smi driver/library mismatch, SSD/HDD 미마운트. Jetson의 AI Hub
  `extracted/`는 0바이트(연구노트 #16 §6.1).
- **이번 가설과 반증 조건:** (a) 서버 루트 디스크 압박은 `factory_safety/data/aihub`(84 GB)와
  `MONAI/physical-ai-research`(478 GB)를 HDD로 옮기면 해소된다. (b) 이동 후 원래 경로의 심볼릭 링크로
  기존 코드가 무수정 동작한다. 반증: 체크섬 불일치, 링크 경유 접근 실패.
- **데이터·환경 버전:** 서버 `keti@10.252.219.59` (`keti-Precision-7920-Tower`), Ubuntu 22.04.5,
  HDD UUID `178b35a2-fa88-49cb-adae-7a7e85f26613`. 작업은 Jetson `keti-kms`에서 SSH 로 수행.
  Jetson HEAD `d84f661` + 미커밋 63개 (변경하지 않음).
- **실행 ID:** 학습 실행 아님. 로그는 서버 `/home/keti/{aihub,monai}_move_{rsync,verify*}.log`.
- **관측 결과 / 해석 / 결정:** 아래.
- **논문 후보에 미치는 영향:** 없음. PC0 의 재실험(G2d~) 선행조건이 완화됨.
- **다음 최소 실험과 통과 조건:** G1-S (handoff §7), 이후 서버 AI Hub 사본의 L2~L4 검증 (handoff §13-4).

---

## 1. 서버 실측 (read-only, 2026-09-17)

| 항목 | 값 | 출처 |
|---|---|---|
| CPU / RAM | 2× Xeon Silver 4214R (48 thread) / 187 GiB | preflight.json |
| GPU | 2× Quadro RTX 6000 24 GB, driver **580.178.04** | 재부팅 후 `nvidia-smi` 정상 (02:46 preflight 시점엔 driver/library mismatch FAIL) |
| 루트 디스크 | 908 GB, 이관 전 830 GB 사용 · **32 GB 여유 · 97 %** | `df` |
| SSD | Micron 2300 1 TB, ext4, `/mnt/data-ssd` (891 GB 여유, 비어 있음) | `findmnt` |
| HDD | ST8000 7.3 TB, ext4, `/mnt/data-hdd` (이관 전 3.5 TB 여유) | `findmnt` |
| fstab | 두 디스크 UUID 항목, `nofail,x-systemd.device-timeout=10s`, 백업 `/etc/fstab.backup.keti-data.1789625611248519500` (15:13:31 KST) | 서버 `/etc/fstab` |
| 재부팅 | 09-17 약 14:40 KST. **NVMe 번호가 뒤바뀜** (root nvme0n1p4 → nvme1n1p4, SSD nvme1n1p1 → nvme0n1p1). UUID fstab 덕에 마운트 정상 | `uptime`, `findmnt` |
| boot-time 마운트 | **미검증** (설정 후 재부팅 안 함) | `SSD_HDD_setup_result.txt` |
| Docker | 29.6.0, compose 5.1.4, keti 접근 가능, 정지 컨테이너 3, 이미지 23 GB | preflight.json |
| torch 환경 | `~/venvs/factory_training` **없음**. `/home/keti/monai_env` (python venv, Py 3.12.13) 에 **torch 2.6.0+cu124, CUDA True**. conda envs: etc/stock/time-llm/uni2ts (torch 없음). `nvcc` 없음 | SSH 실측 |
| 모니터링 | telegraf/influx/grafana 미설치, 3000/8086/8181 리스너 없음 | preflight.json |
| 서버 clone | `/home/keti/factory_safety` = private `mskim0114/factory_safety` main `5feabb5`; `public-upstream` 원격으로 `d84f661` fetch 됨; dirty 51 (전부 `docs/특허/`) | `git` |

HDD 기존 내용(3.5 TB): `TSAD_dataset` 827 GB, `backup_home` 713 GB, `Aihub` 1.8 TB(**2023 감정 데이터, 무관**), `data` 145 GB.

## 2. 서버의 AI Hub #71802 사본은 온전하다 (파일 크기 기준)

Jetson 의 0바이트 문제는 **Jetson 사본만의 문제**였다. 서버 `factory_safety/data/aihub/datasets/extracted/`:

```
Training   csv 99,476 · bin 99,476 · json 99,476    0바이트 0
Validation csv 12,394 · bin 12,394 · json 12,394    0바이트 0
총 18 GB · 파일 mtime 2025-03-10 · 표본 csv 82 B, bin 153,728 B
```

또 `models/AI모델/3.도커이미지/docker44_1.tar` 는 서버에서 **61 GB 완전본**이다(Jetson 은 17.9 GB 에서 잘림).
`train_manager.py` 0바이트 문제(B-6)의 복구 경로가 될 수 있으나 **미확인**.

**한계.** 위는 존재·크기 기준이다. handoff §13-4 의 L2(파싱)·L3(대응)·L4(집계) 검증은 **미실행**이며,
숫자 일치만으로 복구 성공을 선언하지 않는다.

## 3. 이관 결과

두 건 모두 같은 절차: rsync 복사 → `find` 독립 재계산으로 수·바이트 대조 → `rsync -anc` 전수 체크섬
(종료 코드 명시 기록) → 원본 rename → 심볼릭 링크 → 링크 경유 검증 → **검증 통과 후에만** 원본 삭제.

| | `factory_safety/data/aihub` | `MONAI/physical-ai-research` |
|---|---|---|
| 정체 | AI Hub #71802: extracted 18 G + zip 5.4 G + 공식 모델·docker 61 G | 사용자 프로젝트 `KimMyeongSeop/monai-keti` (수술 영상, FSDP): `data/processed` 417 G + `.git` 61 G + 코드 |
| 크기 / 파일 | 88,779,639,866 B / 460,591 | 513,061,936,595 B / 1,645 |
| 복사 | 8분 05초, exit 0 | 40분 27초, exit 0 |
| 수·바이트 재계산 | 양쪽 일치, 0바이트 5/5 | 양쪽 일치, 0바이트 15/15, 심볼릭 링크 0/0 |
| 전수 체크섬 | `RSYNC_EXIT=0`, 차이 0 (1차는 exit 미기록으로 폐기, 2차 재실행) | `RSYNC_EXIT=0`, 차이 0 |
| 링크 경유 검증 | csv·bin 표본 sha256 일치, 파일 수 460,591, `config.py` 하드코딩 경로 접근 OK | 데이터 표본·README sha256 일치, 파일 수 1,645, git HEAD `186352f` 일치 |
| 새 위치 | `/mnt/data-hdd/keti_data/factory_safety/aihub` | `/mnt/data-hdd/keti_data/physical-ai-research` |
| 원본 삭제 | 완료, 잔존 없음 | 완료, 잔존 없음 |

```
루트 디스크   이관 전 830 G 사용 / 32 G 여유 / 97 %  ->  이관 후 269 G 사용 / 593 G 여유 / 32 %   (561 GB 확보)
HDD           3.5 T 사용 / 50 %                  ->  4.0 T 사용 / 2.9 T 여유 / 58 %
```

**실패·예외.** MONAI 링크 교체 1차 실행은 `set -o pipefail` + `ls | head -1` 조합의 SIGPIPE(exit 141)로
변수 계산 단계에서 즉시 중단됐다. 출력이 전혀 없었고 `mv` 이전이므로 상태 변경이 없음을 재실행 시
사전 조건(`SRC` 실제 디렉터리, `OLD` 부재)으로 확인한 뒤 진행했다.

**건드리지 않은 것.** `physical-ai-research/.git` 61 GB(pack 59 GB + 미완료 `tmp_pack` 3개 3 GB)는 그대로
옮겼다. `git gc`/tmp_pack 삭제로 회수 가능하지만 이력 작업이라 사용자 결정 사항(decisions O-107).
`docker` 56 GB, `.cache` 41 GB, `Downloads` 26 GB, `Subject1` 14 GB 미처리.

## 4. 센서 데이터 서버 저장 (Codex, 2026-09-17 15:20 KST)

`/mnt/data-hdd/keti_data/factory_safety/sensor_data/` 에 1회 import 완료.

```
import_20260917T062037Z_a3f4c587   19 run · 270 파일 · 106,054,763 B · status copy_integrity_verified
  sensor_collection   164 파일  (09-03 collector 개발 run 11개)
  smoke_trials         97 파일  (dataset/_smoke 7 trial, baseline 5 포함)
  collection_review     9 파일  (09-17 60초 라이브 run)
검증  source_unchanged=true · destination_verified=true · manifest_sha256 58e23e80…
      .incoming 비어 있음 · originals_retained=true · automatic_upload_configured=false
```

설계 원칙(`sensor_data/README.txt`): Jetson 로컬 우선 기록, 서버 경로 직접 기록 금지, 완료 run 만
checksum 대조 후 `raw/<host>/<import id>/` 로, Jetson 원본 자동 삭제 없음, HDD 1곳은 백업 대체 아님.
**전송을 수행한 스크립트는 Jetson·서버 어디에도 저장되어 있지 않다** (B-7).

## 5. 결정

- 서버 데이터 경로는 심볼릭 링크로 유지한다 (decisions D-015). 서버 clone 의 `config.py`(5feabb5, 09-10
  경로 수정 미포함)가 하드코딩한 `/home/keti/factory_safety/data/aihub/...` 가 그대로 동작한다.
- B-1 은 "Jetson 사본 0바이트" 로 범위를 좁힌다. 서버에서 G2d 는 재추출이 아니라 **L2~L4 검증**이다.
- B-3 은 read-only preflight 완료로 부분 해소. 남은 것: 패키지 목록, `requirements-server.txt`,
  `factory_training` venv 정책 결정(O-108).

## 6. 서버에 작업 반영 및 G1-S (2026-09-18)

### 6.1 push 와 새 clone

Jetson 미커밋 65개(75 항목)를 6개 커밋으로 push 했다: `33b56d1` chore(repo) · `1caf205` fix(train) ·
`68dfac9` docs(paper) · `86d809c` docs(research) · `ea5d41d` feat(jetson) collector · `572cff8` feat(jetson)
dashboard. 75 files, +7,789/−648. 서버에는 `/home/keti/projects/factory_safety` 에 새로 clone 했다
(`572cff8`, remote `private` 추가는 fetch 용). 기존 `/home/keti/factory_safety` 는 무변경(D-018).

### 6.2 데이터 배치

```
HDD 무작위 읽기  1,500쌍(bin+csv)  29.6 s  ->     51 샘플/s      1 epoch = 558,780 파일 읽기
SSD 무작위 읽기  1,500쌍            0.11 s  -> 13,287 샘플/s     (둘 다 page cache 영향 가능, 차이 250배)
```
→ `extracted/` 작업 사본을 SSD 에 복사(rsync exit 0, 459,873 파일, 17,795,317,991 B, HDD 와 수·바이트·0바이트 수 동일).
clone 의 `data/aihub/datasets/extracted` → SSD, 나머지 → HDD 아카이브(D-017). `dataset/_smoke` → HDD import 의
`smoke_trials`. Jetson 논문 run `results/`(48 파일, 363 MB)를 clone 으로 전송 — 기존 clone 의 `results/v2plus`
는 F1 0.9551277 best@21 (08-06 재실행)로 논문 run(0.9550 best@27)과 다르다.

### 6.3 학습 환경 확인

`monai_env`(venv, Py 3.12.13, 2025-12-16 생성, 9.7 GB, 336 패키지): 이름대로 MONAI(의료영상) 기반의
수술 영상 프로젝트(`physical-ai-research`)용 env 다 — monai 1.5.1, monai-generative, nibabel, pydicom, SimpleITK,
transformers 4.57 동거. 그런데 기존 factory_safety 학습도 이 위에서 돌았다(서버 README `conda activate monai_env`,
`logs/reproducibility_test*.log`, 연구노트 #10). `src/` 의존 11개가 전부 있고 CUDA 2장 인식.

**정책 판단:** 기능은 충분하지만 다른 프로젝트와 공유하는 env 라 ENVIRONMENT_POLICY 의 전용 venv 원칙에
어긋난다. 그래서 **`/home/keti/venvs/factory_training` 을 새로 만들고 pin 은 `monai_env` 의 버전을 그대로
옮겼다**(D-019). 결과: Python 3.12.13, 루트 디스크, 5.6 GB, 53 패키지, `PIP_EXIT=0`(11:38:33~11:41:26).

```
pin 11/11 일치   torch 2.6.0+cu124 · torchvision 0.21.0+cu124 · numpy 2.2.6 · scikit-learn 1.8.0 ·
                 matplotlib 3.10.8 · seaborn 0.13.2 · PyYAML 6.0.3 · onnx 1.20.0 · onnxruntime 1.24.4 · tqdm 4.67.1 · pandas 2.3.3
CUDA             torch.version.cuda 12.4 · is_available True · Quadro RTX 6000 ×2
src import       src.data / src.models.v2_plus / window_builder OK · SENSOR_CHANNELS 정본 · V2Plus 2,849,940 params
-m 실행          python -m src.data.scripts.build_windows --dry-run  5/1800/60/49/11 PASS  (handoff 의 미검증 항목 해소)
DataConfig       기본 경로가 clone 기준으로 SSD 작업 사본을 가리킴 (cache/ 는 첫 실행 시 생성)
```

`requirements-server.txt` 는 이전에 pin 없는 자리표시자(791 B, "PENDING: populate only after server
environment audit")였고 위 11개 pin 으로 채웠다. 시스템 python3 은 numpy 1.21.5 (구버전, 사용 안 함).

### 6.4 G1-S — PASS

```
STEP 5  LC_ALL=C sha256sum -c development_baseline_20260904.sha256    85 OK / 0 FAILED
        (한국어 locale 에서 'OK' 가 '성공' 으로 출력돼 1차 grep 이 0 을 반환 — 종료코드는 0 이었음. LC_ALL=C 로 재확인)
STEP 6  build_windows --dry-run 후 정식 실행 (monai_env python)
        trials 5 · snapshots 1800 · windows 60 · valid 49 · invalid 11 · 구조오류 0 · tick-quality 재계산 일치
        출력 processed/development_baseline_v1 38.55 MB, builder_git_commit 572cff8
STEP 7  전후 raw 85 OK · processed/ git 무시 · status 0
```

Jetson 산출물과의 동등성(배열 단위, 09-10 검증과 같은 기준):

| 비교 | 결과 |
|---|---|
| NPZ 60 × 38 배열 = 2,280 | **불일치 0** (shape·dtype·값) |
| `windows.jsonl` 60 레코드 | 환경 종속 3필드 제외 **불일치 0**. `npz_bytes` 는 동일(635,276 등), `npz_sha256` 만 다름, `source_trial_path` 는 서버가 링크 경유 절대경로 |
| `baseline_stats.json` | **바이트 동일** |
| manifest totals / quality_policy / channel_boundary | 동일 |
| NPZ 파일 SHA-256 | **다름** — zip 엔트리 타임스탬프. 파일 해시는 재현 기준이 아니다 |

### 6.5 남은 것

G2d L2~L4 검증(SSD 사본, `factory_training` 으로 실행) → G2e 재실행(EXP-20260907-002). B-7 전송 도구 복원.
서버 두 clone 의 장기 정책 O-109. O-108 은 D-019 로 종결.

## 7. G2d — SSD 작업 사본 계층 검증 (2026-09-18) — PASS

handoff §13-4 의 L2→L3→L4 를 `factory_training` venv 로 전수 실행했다. 스크립트·결과는 서버
`~/review_runs/20260918_g2d/`. 1차 실행은 배포 JSON 의 `meta_info` 가 **list**(길이 1)인 것을 dict 로
가정해 L3 에서 중단됐다(`run_attempt1_failed.log`). 구조를 확인해 고친 뒤 재실행했다 (11:52:38~11:54:06).

| 층 | 검사 | Training | Validation | 실패 |
|---|---|---|---|---|
| L2 | 파일 수 csv/bin/json | 99,476 ×3 | 12,394 ×3 | 0 |
| L2 | 0바이트 | 0 | 0 | 0 |
| L2 | CSV 헤더 `NTC,PM1.0,PM2.5,PM10,CT1,CT2,CT3,CT4` + 데이터 1행 + 8 숫자 필드 | 99,476 | 12,394 | 0 |
| L2 | BIN `np.load` shape (120,160) | 99,476, dtype float64 | 12,394, float64 | 0 |
| L2 | JSON `annotations[0].tagging[0].state`, `meta_info[0].duration_time` | state {0:49,285 1:21,189 2:21,322 3:7,680}, duration 전부 '1' | {0:5,643 1:2,892 2:2,869 3:990}, 전부 '1' | 0 |
| L3 | basename 차집합 csv↔bin↔json | 0 | 0 | 0 |
| L3 | 파일명 규약 `^(agv|oht)\d+_\d{4}_\d{6}$` | 위반 0 | 0 | 0 |
| L3 | 파일명 ↔ `collection_date`/`collection_time` | 99,476 일치 | 12,394 일치 | 0 |
| L4 | `session_index.py` 강제 재생성 (gap 120) | 303 session · 32 장비 · 9,313 window | 39 · 4 · 1,157 | 0 |
| L4 | 인접 Δ / window span | 99,173쌍 전부 1 s / 9,313 전부 29 s | 12,355쌍 전부 1 s / 1,157 전부 29 s | 0 |
| L4 | 장비 교집합 | agv01~16 · oht01~16 | agv17·18 · oht17·18 | 0 |

라벨 분포(state 히스토그램)는 논문 Table 3 의 클래스 비율 49.5 / 21.3 / 21.4 / 7.7 % 와 일치한다.
검증 전후 SSD 사본 지문(`find … | sha256sum`) 동일 `d4c6b1bb42d01b02`. 재생성된 `cache/session_index_*.pkl`
(11:54, mode 0600)은 09-10 서명 검증 코드로 만들어졌다.

**부수 관측 N15 — 열화상 값 범위.** BIN 전수의 min/max 는 Training **−110.29 ~ 172.06 °C**, Validation
14.04 ~ 138.96 °C 다. 논문 `:133` 은 "약 31 °C ~ 146 °C, clipping 불필요" 라 쓰고 `ThermalStats` 는
(30.98, 146.10) 이다. 범위 밖 값은 정규화 후 [0,1] 을 벗어난다. 데이터 무결성 문제는 아니지만 (a) 어느
장비·시점의 프레임인지, (b) 센서 아티팩트인지 실제 온도인지, (c) 학습에 미친 영향은 **미확인**이다.
정량화 결과는 §7.1 에 이어 적는다. 논문 문장은 원인 확인 전에는 고치지 않는다(claim_evidence CE-047).

### 7.1 N15 정량화 (전수, 2026-09-18)

| | Training (99,476 프레임) | Validation (12,394 프레임) |
|---|---:|---:|
| min < 30.98 °C 인 프레임 | 153 (0.15 %) | 31 (0.25 %) |
| max > 146.10 °C 인 프레임 | 59 (0.06 %) | 0 |
| 음수 값 포함 프레임 | 11 | 0 |
| 범위 밖 픽셀 | 559,560 + 873 = 560,433 (**0.029 %**) | 360,050 (**0.15 %**) |
| 전역 min / max | −110.29 / 172.06 °C | 14.04 / 138.96 °C |

해석(관찰과 추론 분리). **관찰:** 범위 밖 값은 프레임·픽셀 모두 1 % 미만이다. Validation 의 31개 프레임은
평균 11,600 픽셀(프레임의 약 60 %)이 30.98 미만이라 "국소 잡음"이 아니라 **장면 전체가 차가운 프레임**
(예: 배경 14 °C) 으로 보인다. Training 의 음수 11개 프레임(−110 °C)은 물리적으로 불가능한 값이라 센서·변환
아티팩트일 가능성이 높다. **추론(미검증):** 정규화 후 −1.22 ~ +1.23 으로 [0,1] 을 벗어나지만 프레임 수가
적어 학습 지표에 미친 영향은 작았을 것이다 — 이것은 재실행(G2e) 전에는 확인할 수 없다.

**논문에 대한 함의:** `:133` 의 "약 31~146 °C" 는 대체로 맞고, **"clipping 이 필요 없다" 는 부정확**하다
(212 + 31 프레임이 범위 밖). 어느 장비·시점인지와 처리 방침(clip / 제외 / 통계 재산정)은 별도 조사 뒤
결정한다. 지금은 본문을 고치지 않는다(CE-047).
